import SwiftUI

@main
struct DesktopTrackerApp: App {
    var body: some Scene {
        WindowGroup("TeamSight Tracker") {
            ContentView()
        }
        .defaultSize(width: 1000, height: 700)
    }
}
